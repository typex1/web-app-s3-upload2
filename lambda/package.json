{
  "name": "presigned-url-generator",
  "version": "1.0.0",
  "description": "Lambda function to generate pre-signed URLs for S3 uploads",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "aws-sdk": "^2.1048.0"
  },
  "devDependencies": {
    "jest": "^27.4.7"
  }
}